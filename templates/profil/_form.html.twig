{{ form_start(registrationForm) }}
{{ form_widget(registrationForm) }}</div><div class="flex items-center justify-between">
<button type="submit" class="w-full bg-primary-600 hover:bg-primary-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">S'enregistrer</button></div>{{ form_end(registrationForm) }}